import { Inconsolata, Rubik_Vinyl } from 'next/font/google'

export const inconsolata = Inconsolata({
  subsets: ['latin']
})

export const rubik_vinyl = Rubik_Vinyl({
  subsets: ['latin'],
  weight: "400"
})
